<UserControl x:Class="_3D_VisionSource.Viewer.Viewer3DControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:dx="clr-namespace:HelixToolkit.Wpf.SharpDX;assembly=HelixToolkit.Wpf.SharpDX">
    <Grid>
        <dx:Viewport3DX x:Name="Viewport"
                    ShowCoordinateSystem="True"
                    ShowViewCube="True"
                    EnableDesignModeRendering="False"
                    Background="White">
            <!-- 카메라 -->
            <dx:Viewport3DX.Camera>
                <dx:PerspectiveCamera Position="0,-200,150"
                              LookDirection="0,200,-150"
                              UpDirection="0,0,1"
                              FieldOfView="45" />
            </dx:Viewport3DX.Camera>

            <!-- 조명 (포인트는 조명 영향 거의 없음, 있어도 무방) -->
            <dx:DirectionalLight3D Direction="-0.3,-0.3,-1" Color="White" />
            <dx:AmbientLight3D     Color="#FF606060" />
            <!-- (PointGeometryModel3D는 코드에서 생성/추가) -->
        </dx:Viewport3DX>
    </Grid>
</UserControl>